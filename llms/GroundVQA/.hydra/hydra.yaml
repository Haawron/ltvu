hydra:
  run:
    dir: /data/gunsbrother/prjs/ltvu/llms/GroundVQA
  sweep:
    dir: multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    _target_: hydra._internal.core_plugins.basic_launcher.BasicLauncher
  sweeper:
    _target_: hydra._internal.core_plugins.basic_sweeper.BasicSweeper
    max_batch_size: null
    params: null
  help:
    app_name: ${hydra.job.name}
    header: '${hydra.help.app_name} is powered by Hydra.

      '
    footer: 'Powered by Hydra (https://hydra.cc)

      Use --hydra-help to view Hydra specific help

      '
    template: '${hydra.help.header}

      == Configuration groups ==

      Compose your configuration from those groups (group=option)


      $APP_CONFIG_GROUPS


      == Config ==

      Override anything in the config (foo.bar=value)


      $CONFIG


      ${hydra.help.footer}

      '
  hydra_help:
    template: 'Hydra (${hydra.runtime.version})

      See https://hydra.cc for more info.


      == Flags ==

      $FLAGS_HELP


      == Configuration groups ==

      Compose your configuration from those groups (For example, append hydra/job_logging=disabled
      to command line)


      $HYDRA_CONFIG_GROUPS


      Use ''--cfg hydra'' to Show the Hydra config.

      '
    hydra_help: ???
  hydra_logging:
    version: 1
    root: null
    disable_existing_loggers: false
  job_logging:
    version: 1
    root: null
    disable_existing_loggers: false
  env: {}
  mode: RUN
  searchpath: []
  callbacks: {}
  output_subdir: .hydra
  overrides:
    hydra:
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_1
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_2
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_3
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_4
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_5
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_6
    - hydra.run.dir="/data/gunsbrother/prjs/ltvu/llms/GroundVQA"
    - hydra.job.name=train_ddp_process_7
    - hydra.mode=RUN
    task:
    - batch_flag='0'
    - model=input_embed
    - model.model_variant=input_concat
    - model.env_ext_variant=id
    - dataset.nlq_train_splits=[NLQ_train]
    - dataset=egovlp_internvideo_llavacap_sbert
    - dataset.batch_size=6
    - dataset.llava.llava_dir='data/features/00_cheat_env_binary'
    - dataset.llava.feature_aggregation=cross
    - optim.optimizer.lr=1e-5
    - trainer.max_epochs=20
    - trainer.gpus=8
    - trainer.enable_progress_bar=True
    - +trainer.checkpoint_path='checkpoints/GroundVQA_B-NLQ_NaQ-finetune_NLQ-VLG-val_R1_03=29.7.ckpt'
  job:
    name: train_ddp_process_7
    chdir: null
    override_dirname: +trainer.checkpoint_path='checkpoints/GroundVQA_B-NLQ_NaQ-finetune_NLQ-VLG-val_R1_03=29.7.ckpt',batch_flag='0',dataset.batch_size=6,dataset.llava.feature_aggregation=cross,dataset.llava.llava_dir='data/features/00_cheat_env_binary',dataset.nlq_train_splits=[NLQ_train],dataset=egovlp_internvideo_llavacap_sbert,model.env_ext_variant=id,model.model_variant=input_concat,model=input_embed,optim.optimizer.lr=1e-5,trainer.enable_progress_bar=True,trainer.gpus=8,trainer.max_epochs=20
    id: ???
    num: ???
    config_name: base
    env_set: {}
    env_copy: []
    config:
      override_dirname:
        kv_sep: '='
        item_sep: ','
        exclude_keys: []
  runtime:
    version: 1.3.2
    version_base: '1.3'
    cwd: /data/gunsbrother/prjs/ltvu/llms/GroundVQA
    config_sources:
    - path: hydra.conf
      schema: pkg
      provider: hydra
    - path: /data/gunsbrother/prjs/ltvu/llms/GroundVQA/config
      schema: file
      provider: main
    - path: ''
      schema: structured
      provider: schema
    output_dir: /data/gunsbrother/prjs/ltvu/llms/GroundVQA
    choices:
      model: input_embed
      dataset: egovlp_internvideo_llavacap_sbert
      hydra/env: default
      hydra/callbacks: null
      hydra/job_logging: none
      hydra/hydra_logging: none
      hydra/hydra_help: default
      hydra/help: default
      hydra/sweeper: basic
      hydra/launcher: basic
      hydra/output: default
  verbose: false
